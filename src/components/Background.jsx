import React from 'react';
import './Background.css'; // Create this file for background-specific styles

const Background = () => {
  return (
    <div className="app-background">
      {/* You can add background elements here if needed, like a gradient, image, etc. */}
      {/* For now, it's just a placeholder div that fills the background */}
    </div>
  );
};

export default Background;